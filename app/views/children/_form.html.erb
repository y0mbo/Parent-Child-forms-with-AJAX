<%= form_for [@parent, @parent.children.build], :remote => true do |f| %>
  <div class="field">
    <%= f.label :name %><%=f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
